# Missing Data Handling

Missing financial inputs are represented as unavailable rather than fabricated. Analytical features should propagate unavailable inputs instead of silently substituting synthetic values.
